<%if obj.errors.any? %>
<div class="alert alert-danger alert-dismissible">
	<button aria-hidden="true" class="close" data-dismiss="alert" type="button">×</button>
	<h4><i class="icon fa fa-warning"></i> <%= pluralize(obj.errors.count, "error") =%>, Please make sure all required fields are filled out correctly!</h4>
	<ul>
		<% obj.errors.full_messages.each do |msg| %>
		<li><%= msg %></li><%end %>
	</ul>
</div>
<%end %>