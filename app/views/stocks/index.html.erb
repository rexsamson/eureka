		<section class="content-header">
			<%= link_to raw("<i class=\"fa fa-plus\" aria-hidden=\"true\"> </i> Create New Journal"), new_journal_path, :class => "btn btn-primary btn-flat "%>
			<ol class="breadcrumb">
				<li>
					<a href="#"><i class="fa fa-dashboard"></i> Home</a>
				</li>
				<li>
					<a href="#">Journals</a>
				</li>
				<li class="active">Index page</li>
			</ol>
		</section><!-- Main content -->
		<section class="content">
			<%= render 'shared/flash' %>
			
			<div class="box box-primary">
				<!-- /.box-header -->
				<div class="box-header">
					
		        <h3  class="box-title">Journals <small>List</small></h3>
		        <div class="pull-right">
					<%= form_tag(journals_path, :method => "get", id: "search-form") do %>
						<table class="table-search">
							<tr>
								<td class="td-search"><b class="pull-right">Search:</b></td>
								<td class="td-search"><%= text_field_tag :search, params[:search], class: "form-control input-sm search-field" %></td>
							</tr>
						</table>
						<%= submit_tag "search", :style => "display: none;" %>
					<% end %>
				</div>
		        </div>
				<div class="box-body" id="dttable_filter">
				
					<table class="table table-bordered table-hover" >
						<thead class="thead"
							<tr>
								<th style="width: 100px">Code</th>
								<th style="width: 100px">Date</th>
								<th style="width: 70px">Status</th>
								<th>Description</th>
								<th style="width: 125px">Action</th>
							</tr>
						</thead>
						<tbody>
							<%@journals.each do |journal| %>
							<tr>
								<td><%= journal.code %></td>
								<td><%= journal.date %></td>
								<td><span class="label <%= journal.status %>"><%= journal.status %></span></td>
								<td><%= journal.description %></td>
								<td>
									<table>
									<tr><td><%= link_to raw("<i class=\"fa fa-print\" aria-hidden=\"true\"></i>"), journal_path(journal), :class => "btn btn-success btn-sm " %></td>
	                                <td><%= link_to raw("<i class=\"fa fa-edit\" aria-hidden=\"true\"></i>"), edit_journal_path(journal), :class => "btn btn-warning btn-sm" %> </td>
	                                <td><%= link_to raw("<i class=\"fa fa-gavel\" aria-hidden=\"true\"> </i>"), journal_path(journal), method: :delete, data: {confirm: "Are you sure?"}, :class => "btn btn-danger btn-sm" %></td>
	                                </tr>
	                                </table>
                                </td>
							</tr>	
							<% end %>
						</tbody>
					</table>
				

				</div><!-- /.box-body -->
				<div class="box-footer ">
				
				</div>

			</div><!-- /.box-end -->
			
		</section>
		